# Art + Zap Weekend — Spreadsheet Update Instructions

I cannot directly edit your Google Sheet (it requires sign-in), but everything you need to update it is in this folder as ready-to-paste CSV files. Each CSV maps to one tab in your Sheet.

## How to update your Google Sheet

For each CSV file below:

1. Open your Google Sheet
2. Create a new tab (or use the existing one if it matches)
3. Click cell **A1**
4. **File → Import → Upload → select the CSV → "Replace current sheet"** *(or)*
5. Or open the CSV in any text editor, copy all, paste into A1 of the tab, then **Data → Split text to columns → Comma**

## Tabs to add / replace

| Tab name | CSV file | Purpose |
|---|---|---|
| `Sponsors — Tiers` | `tabs/sponsors-tiers.csv` | The 9 sponsor slots, what each gets, and tracking |
| `Sponsors — Pipeline` | `tabs/sponsors-pipeline.csv` | Outreach tracker — who you're approaching, status, amounts |
| `In-Kind` | `tabs/in-kind.csv` | In-kind asks tracker (venue, A/V, food, etc.) |
| `Podcaster Hosts` | `tabs/podcaster-hosts.csv` | Host outreach tracker — 4 confirmed + 2 backups |
| `Artists` | `tabs/artists.csv` | The 21 artist roster, contact, day, slot, status |
| `Schedule — Saturday` | `tabs/schedule-saturday.csv` | Minute-by-minute Saturday rundown |
| `Schedule — Sunday` | `tabs/schedule-sunday.csv` | Minute-by-minute Sunday rundown |
| `Budget` | `tabs/budget.csv` | Cash + in-kind running totals against tiered scenarios |
| `Tasks — Owners` | `tabs/tasks-owners.csv` | One-row-per-task owner board (you, 2 volunteers, board) |

## Recommended workflow

1. **Don't delete your existing tabs** — duplicate them first as `archive-old-7day` so you can reference them later.
2. Import the CSVs above as new tabs.
3. Fill in the [TBD] fields with your real names, dates, contact info.
4. Color-code statuses (Confirmed = green, In conversation = yellow, Declined = gray) using **Format → Conditional formatting**.
5. Pin the `Sponsors — Pipeline` tab as your daily working view between now and the event.
